/** Office map: 40×30 tiles @ 48px → 1920×1440 canvas (was 32px / muddy stretch). */
export const TILE_SIZE = 48;
export const MAP_COLS = 40;
export const MAP_ROWS = 30;
export const MAP_W = MAP_COLS * TILE_SIZE;
export const MAP_H = MAP_ROWS * TILE_SIZE;

/** Character sprite sheet frame (3× legacy 16×24). */
export const CHAR_FRAME_W = 48;
export const CHAR_FRAME_H = 72;
