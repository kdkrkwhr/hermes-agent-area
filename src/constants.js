/** Office map: 40×30 tiles @ 32px → 1280×960 canvas. */
export const TILE_SIZE = 32;
export const MAP_COLS = 40;
export const MAP_ROWS = 30;
export const MAP_W = MAP_COLS * TILE_SIZE;
export const MAP_H = MAP_ROWS * TILE_SIZE;

/** Character sprite sheet frame (2× legacy 16×24). */
export const CHAR_FRAME_W = 32;
export const CHAR_FRAME_H = 48;
